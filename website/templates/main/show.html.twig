{% extends 'base.html.twig' %}
{% import '@SpipuUi/macro/buttons.html.twig' as buttons %}

{% set menuCurrentItem="home" %}

{% block header_title %}Boiler Reader - Show{% endblock %}
{% block main_title %}Show{% endblock %}

{% block main_actions %}
    {{ buttons.back(path('app_home')) }}
    {{ buttons.defaultLink(path('app_buffer_delete', {'id': manager.resource.id}), 'Delete', 'danger', 'trash') }}
{% endblock main_actions %}

{% block body %}{{ renderManager(manager) }}{% endblock body %}
